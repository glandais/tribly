# API proxy target for the Vite dev/preview server.
# If unset (no .env file), defaults to https://www.pedalons.fr (production API).
# Copy this file to .env and uncomment to target a local backend instead.

# VITE_API_TARGET=http://localhost:8080

# Filtered at build time by Maven resource filtering.
# s3mock.version tracks the s3mock-testcontainers Maven dependency (managed by Dependabot),
# so the S3Mock Docker image tag never drifts from the library version.
s3mock.version=${s3mock-testcontainers.version}

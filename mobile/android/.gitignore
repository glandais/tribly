gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Fastlane (metadata/ is kept; it's the source of truth for Play Store listing)
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output

# Bundler installs fastlane here (BUNDLE_PATH in .bundle/config) — Homebrew's
# shared gem dir symlinks read-only files into the Cellar, so a global install
# dies with EACCES regenerating gem plugins.
vendor/bundle/
